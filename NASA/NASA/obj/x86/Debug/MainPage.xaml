<Page x:ConnectionId='1'
    x:Class="NASA.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NASA"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:NASA.Models"
    xmlns:viewmodels="using:NASA.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <viewmodels:DaysViewModel/>
    </Page.DataContext>
    <Page.Resources>
        <CollectionViewSource x:ConnectionId='2' x:Name="DaysCollection"                              />
        <DataTemplate x:ConnectionId='3' x:Name="DayTemplate"                              >
            <TextBlock x:ConnectionId='4'                       FontSize="32"/>
        </DataTemplate>
    </Page.Resources>
    <Page.TopAppBar>
        <CommandBar>
            <AppBarButton Icon="Setting" Label="About" ToolTipService.ToolTip="About" AutomationProperties.HelpText="About the application"/>
            <AppBarSeparator/>
            <CommandBar.SecondaryCommands>
                <AppBarButton Label="Exit" ToolTipService.ToolTip="Exit" AutomationProperties.HelpText="Exit"/>
            </CommandBar.SecondaryCommands>
        </CommandBar>
    </Page.TopAppBar>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="130"/>
            <RowDefinition Height="441*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="315"/>
            <ColumnDefinition Width="1193*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <DatePicker x:ConnectionId='6' x:Name="datePickStart" Header="Start Date:" HorizontalAlignment="Center"/>
            <DatePicker x:ConnectionId='7' x:Name="datePickEnd" Header="End Date:" HorizontalAlignment="Center"/>
        </StackPanel>
        <ScrollViewer Grid.Row="1" Grid.Column="0">
            <ListView x:ConnectionId='5' 
                ItemsSource="{Binding Source={StaticResource DaysCollection}}"
                ItemTemplate="{Binding Source={StaticResource DayTemplate}}"
                                                                       >

            </ListView>
        </ScrollViewer>
            



    </Grid>
</Page>

